# Frontend has no secrets — the Django backend owns Google OAuth, Sheets, and Groq.
# Locally the Vite dev server proxies /api → http://127.0.0.1:8000 (see vite.config.js).
#
# === Required ===
# (none — no VITE_* env vars are required for normal development)
#
# === Optional ===
# (none currently)
